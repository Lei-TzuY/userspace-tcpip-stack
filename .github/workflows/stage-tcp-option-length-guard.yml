name: Stage TCP option length guard

on:
  push:
    branches:
      - stage/tcp-option-length-guard

permissions:
  contents: write

jobs:
  stage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Apply staged source and regressions
        run: python .github/stage_tcp_option_length_guard.py
      - name: Format
        run: cargo fmt --all
      - name: Check diff
        run: git diff --check
      - name: Focused TCP option tests
        run: cargo test --test test_tcp_option_validation --verbose
      - name: Existing TCP tests
        run: cargo test tcp::tests --verbose
      - name: Full all-target suite
        run: cargo test --all-targets --verbose
      - name: Release build
        run: cargo build --release --verbose
      - name: Commit validated source changes
        shell: bash
        run: |
          git config user.name 'LeiZ'
          git config user.email '52287354+Lei-TzuY@users.noreply.github.com'
          git add src/tcp.rs tests/test_tcp_option_validation.rs
          git commit -m 'fix(tcp): reject malformed option lengths'
          git push
