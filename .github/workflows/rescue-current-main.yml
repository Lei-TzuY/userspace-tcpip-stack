name: Rescue workflow probe

on:
  push:
    branches:
      - fix/restore-current-main-20260905

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe
        run: echo rescue-workflow-ok
